local spreetTOUCH = game:GetService("Workspace").Aboveground.RaiderSpawn.RaiderSpawnDetails.SpreetBox.MeshPart
local char = game.Players.LocalPlayer.Character.HumanoidRootPart

if game.Workspace.Aboveground then
firetouchinterest(char, spreetTOUCH, 0)
wait()
firetouchinterest(char, spreetTOUCH, 1)
end